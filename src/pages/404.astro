---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Page Not Found | OpenedFile" description="The page you're looking for doesn't exist.">
  <div class="mx-auto flex min-h-[60vh] max-w-2xl flex-col items-center justify-center px-4 py-20 text-center">
    <!-- 404 number -->
    <div class="text-8xl font-bold text-primary-600/20 dark:text-primary-400/20 sm:text-9xl select-none">404</div>

    <!-- Icon -->
    <div class="mt-6 flex h-16 w-16 items-center justify-center rounded-2xl bg-primary-100 dark:bg-primary-900/40">
      <svg class="h-8 w-8 text-primary-600 dark:text-primary-400" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24">
        <path d="M15 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7Z"></path>
        <path d="M14 2v4a2 2 0 0 0 2 2h4"></path>
        <line x1="9" y1="13" x2="15" y2="13"></line>
      </svg>
    </div>

    <!-- Messages in both languages -->
    <h1 class="mt-6 text-2xl font-bold text-gray-900 dark:text-white sm:text-3xl">
      <span id="msg-en">Page Not Found</span>
      <span id="msg-ja" class="hidden">ページが見つかりません</span>
    </h1>

    <p class="mt-3 text-base text-gray-500 dark:text-gray-400 max-w-md">
      <span id="desc-en">The page you're looking for doesn't exist or has been moved.</span>
      <span id="desc-ja" class="hidden">お探しのページは存在しないか、移動された可能性があります。</span>
    </p>

    <!-- Navigation links -->
    <div class="mt-8 flex flex-col gap-3 sm:flex-row">
      <a
        id="link-home"
        href="/"
        class="inline-flex items-center justify-center gap-2 rounded-lg bg-primary-600 px-5 py-2.5 text-sm font-medium text-white hover:bg-primary-700 transition-colors"
      >
        <svg class="h-4 w-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
          <path d="m3 9 9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"></path>
          <polyline points="9 22 9 12 15 12 15 22"></polyline>
        </svg>
        <span id="link-home-en">Back to Home</span>
        <span id="link-home-ja" class="hidden">トップページへ</span>
      </a>
      <a
        id="link-tools"
        href="/"
        class="inline-flex items-center justify-center gap-2 rounded-lg border border-gray-200 bg-white px-5 py-2.5 text-sm font-medium text-gray-700 hover:border-primary-300 hover:bg-primary-50 dark:border-gray-700 dark:bg-gray-800 dark:text-gray-200 dark:hover:border-primary-600 dark:hover:bg-primary-950 transition-colors"
      >
        <svg class="h-4 w-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
          <path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"></path>
        </svg>
        <span id="link-tools-en">Browse Tools</span>
        <span id="link-tools-ja" class="hidden">ツール一覧を見る</span>
      </a>
    </div>
  </div>

  <!-- Language detection script -->
  <script is:inline>
    (function() {
      var lang = navigator.language || navigator.userLanguage || '';
      if (lang.startsWith('ja')) {
        document.getElementById('msg-en').classList.add('hidden');
        document.getElementById('msg-ja').classList.remove('hidden');
        document.getElementById('desc-en').classList.add('hidden');
        document.getElementById('desc-ja').classList.remove('hidden');
        document.getElementById('link-home-en').classList.add('hidden');
        document.getElementById('link-home-ja').classList.remove('hidden');
        document.getElementById('link-tools-en').classList.add('hidden');
        document.getElementById('link-tools-ja').classList.remove('hidden');
        document.getElementById('link-home').href = '/ja/';
        document.getElementById('link-tools').href = '/ja/';
      }
    })();
  </script>
</BaseLayout>
